# auto-outorga-granted-capture

## Development

- `cp .env.example .env` And edit
- `npm i`

# Usage

- `npm start`
- `npm start iterative` to run as interative mode
- `npm start no-send-email` to don't send mail (just check)
- `npm start date=2026-04-01` to use another date than today
